
// Runs once before all tests in the current suite
before(() => {
  // This code will run before each test in the suite
});


// Runs before each test in the current suite
beforeEach(() => {
  // This code will run before each test in the suite
});

// Runs after each test in the current suite
afterEach(() => {
  // This code will run after each test in the suite
});

// Runs once after all tests in the current suite
after(() => {
  // This code will only run once, after all tests in the suite
});
